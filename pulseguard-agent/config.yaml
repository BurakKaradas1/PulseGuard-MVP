agent:
  interval: 3s
  collector_url: "http://localhost:8080/api/v1/events"

thresholds:
  cpu: 80
  ram: 85
  disk: 90

checks:
  system_integrity: true
  analysis_detection: true
  network_status: true